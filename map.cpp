#include <map>
#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(void)
{
    map<string, vector<string>> family;
    string first;
    string last;
    while(cin >> first)
    {
        cin >> last;


    }

}
